##################################
######## Spot on Plots ###########
##################################

#Let's just have a look on some examples for plots:

#these commands are all included in the package graphics
example(plot)
example(matlines)
example(image)

#there are packages especially for graphics, e.g. lattice
library("lattice")
example(cloud)

#and some packages for special applications
install.packages("waterfall")
library("waterfall")
example(waterfallchart)
